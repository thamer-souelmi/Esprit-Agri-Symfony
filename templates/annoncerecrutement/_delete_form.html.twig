<form method="post" action="{{ path('app_annoncerecrutement_delete', {'idrecurt': annoncerecrutement.idrecurt}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annoncerecrutement.idrecurt) }}">
    <button class="btn">Delete</button>
</form>
