<form method="post" action="{{ path('app_traitementmedicale_delete', {'id': traitementmedicale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traitementmedicale.id) }}">
    <div class="mt-2">
    <button class="btn btn-secondary rounded-pill py-2 px-4">Supprimer</button>
  
</form>
