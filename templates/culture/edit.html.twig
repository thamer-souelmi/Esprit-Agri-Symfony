{% extends 'culturefront.html.twig'%}

{% block title %}Modifier Culture
{% endblock %}

{% block body %}
	<h1>Modifier Culture</h1>

	{{ include('culture/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_culture_index') }}" class="btn btn-primary">Retourner au list</a>

	{{ include('culture/_delete_form.html.twig') }}
{% endblock %}
