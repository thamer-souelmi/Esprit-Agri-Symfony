<form method="post" action="{{ path('app_annonceinvestissement_delete', {'idannonce': annonceinvestissement.idannonce}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonceinvestissement.idannonce) }}">
    <button type="submit" class="btn btn-danger rounded-pill py-2 px-4">Supprimer</button>
</form>
