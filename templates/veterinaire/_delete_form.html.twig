





<form method="post" action="{{ path('app_veterinaire_delete', {'idvet': veterinaire.idvet}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veterinaire.idvet) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
