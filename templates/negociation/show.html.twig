{% extends 'nego.html.twig' %}



{% block body %}
    

    <table class="table">
        <tbody>
            
            <tr>
                <th>Montantpropose</th>
                <td>{{ negociation.montantpropose }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ negociation.message }}</td>
            </tr>
            <tr>
                <th>Etatnego</th>
                <td>{{ negociation.etatnego }}</td>
            </tr>
            <tr>
                <th>Datenegociation</th>
                <td>{{ negociation.datenegociation ? negociation.datenegociation|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>
<div class="form-group row">
                    <div class="col-sm-20 mb-3 mb-sm-0">
                        <div class="my-2"></div>
                             <a href="{{ path('app_negociation_edit', {'id': negociation.id}) }}">Modifier</a>
                        </div>  
                     </div>
                </div>
{{ include('negociation/_delete_form.html.twig') }}
   <div class="form-group row">
                    <div class="col-sm-20 mb-3 mb-sm-0">
                        <div class="my-2"></div>
                        <a href="{{ path('app_negociation_index') }}" class="btn btn-light btn-icon-split btn-lg">
                            <span class="icon text-gray-600">
                                <i class="fas fa-arrow-left"></i>
                            </span>
                            <span class="text">Retour Négociations</span>
                        </a>
                    </div>  
   </div>
                

    
{% endblock %}
