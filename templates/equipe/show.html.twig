{% extends 'prod.html.twig' %}

{% block banner_image %}
    {{ asset('img/recru.jpg') }}
{% endblock %}
{% block product_heading %}
    <h1 class="display-3 text-white mb-4 animated slideInDown">Equipe </h1>
{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom equipe</th>
                <td>{{ equipe.NomEquipe}}</td>
            </tr>
            <tr>
                <th>Tache  attribut</th>
                <td>{{ equipe.tacheattribut }}</td>
            </tr>
            <tr>
                <th>Date debut</th>
                <td>{{ equipe.datedebut ? equipe.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ equipe.duree }}</td>
            </tr>
        </tbody>
    </table>

<div class="text-center">
<a class="btn btn-primary"  href="{{ path('app_equipe_edit', {'idEquipe': equipe.idEquipe}) }}">modifier</a>
   </div>
    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
