{% extends 'prod.html.twig' %}

{% block title %}Equipe{% endblock %}

{% block body %}
    <h1>Equipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom equipe</th>
                <td>{{ equipe.NomEquipe}}</td>
            </tr>
            <tr>
                <th>Tache  attribut</th>
                <td>{{ equipe.tacheattribut }}</td>
            </tr>
            <tr>
                <th>Date debut</th>
                <td>{{ equipe.datedebut ? equipe.datedebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ equipe.duree }}</td>
            </tr>
        </tbody>
    </table>


<a href="{{ path('app_equipe_edit', {'idEquipe': equipe.idEquipe}) }}">modifier</a>

    {{ include('equipe/_delete_form.html.twig') }}
{% endblock %}
