<form method="post" action="{{ path('app_ouvrier_delete', {'idouvrier': ouvrier.idouvrier}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ouvrier.idouvrier) }}">
    <button class="btn">Delete</button>
</form>
