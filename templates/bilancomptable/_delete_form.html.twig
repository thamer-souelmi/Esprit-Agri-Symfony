<form method="post" action="{{ path('app_bilancomptable_delete', {'idbilanc': bilancomptable.idbilanc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bilancomptable.idbilanc) }}">
    <button class="btn">Delete</button>
</form>
